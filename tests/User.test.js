const { TestWatcher } = require("jest");

describe ('User Object Properties', () => {

const user1 = new User(name)

test("User is an object", () => {
    expect(user1).toBe('object')
})

test("User is a username", () => {
    expect(user1).toBe('username')
})

})



